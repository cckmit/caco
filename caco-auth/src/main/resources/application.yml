server:
  port: 4000
spring:
  application:
    name: caco-auth
  profiles:
    active: dev

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://caco:caco123@127.0.0.1:3000/eureka/